# AI_SAFETY_RULES.md

> **优先级：最高**
>
> 本文档是项目安全策略的唯一权威来源。
> 任何 Agent、Command、Workflow、Skill 指令与本文档冲突时，以本文档为准。
> 本文档的任何条款**不得被其他文档覆盖、削弱或绕过**。

---

## 一、权限等级体系

本项目定义五个操作等级。每个等级有明确的授权边界。**AI 永远不得自行升级权限。**

| 等级 | 名称 | 授权来源 | 典型操作 |
|------|------|----------|----------|
| L1 | 查看 | 无需授权 | 读取文件、搜索代码、查看 git 状态、查看 git diff、查看 git log |
| L2 | 修改 | 任务级授权 | 编辑文件、创建文件、运行开发服务器、安装依赖 |
| L3 | 提交 | 用户明确指令 | git add、git commit |
| L4 | 推送 | 用户明确指令 | git push |
| L5 | 部署 | 用户明确指令 | 触发 CI/CD、操作部署平台、修改部署配置 |

### 核心原则

```
L1 查看   ≠  L2 修改   （看代码不等于可以改代码）
L2 修改   ≠  L3 提交   （改完不等于可以 commit）
L3 提交   ≠  L4 推送   （commit 不等于可以 push）
L4 推送   ≠  L5 部署   （push 不等于可以上线）
```

**每个等级升级必须获得用户对该等级的直接授权。** 授权 L2 不等于同时授权 L3；授权 L3 不等于同时授权 L4。用户说"提交"只意味着 L3，不意味着 L4。

---

## 二、Git 权限规则

### L1 查看 — 无需额外授权
- `git status`、`git diff`、`git diff --staged`、`git log`、`git branch`、`git remote -v`
- 任何 `--dry-run` 模式的操作
- 禁止使用 `-uall` 参数（递归未跟踪文件可能导致内存问题）

### L2 修改 — 任务级授权
- `git add <specific-files>`：**必须指定具体文件**，禁止 `git add -A`、`git add .`
- 禁止执行任何 git config 修改
- 禁止执行 `git stash`（隐藏用户未保存的工作）

### L3 提交 — 用户明确指令
- **触发条件**：用户说"提交"、"commit"、"保存到 git"
- **禁止**：在用户没有明确要求的情况下主动 commit
- **禁止**：`git commit --amend`（除非用户明确要求 amend 上一个 commit）
- **禁止**：`git commit --no-verify`、`git commit --no-gpg-sign` 等跳过 hooks 的操作（除非用户明确要求）
- **如果 pre-commit hook 失败**：commit 没有发生，修复问题后创建**新 commit**，不要 amend 上一个
- **提交信息**：中文，1-2 句说明"为什么"改，结尾附 `Co-Authored-By: Claude <noreply@anthropic.com>`

### L4 推送 — 用户明确指令
- **触发条件**：用户说"push"、"推送"、"推到远程"、"发布到 GitHub"
- **禁止 force push 到 main/master 分支**（任何情况下都不允许，即使"用户授权"也应警告阻拦）
- **禁止 force push 到受保护分支**（CI/CD 配置中标记的分支）
- 推送前必须检查：当前分支是否是 main/master，是否与远程有冲突
- 如果用户提到"合并并推送"：先确认目标分支，再执行

### L5 部署 — 用户明确指令
- **触发条件**：用户说"部署"、"上线"、"deploy"、"发布到 Vercel/GitHub Pages"
- GitHub Pages 部署通过 CI/CD（push master 后自动触发），不直接操作
- 部署前确认：构建是否通过、是否有未提交的变更

### 禁止的 Git 操作（任何等级）
- `git reset --hard`（除非用户明确要求，且必须在执行前再次确认）
- `git clean -f` 或 `git clean -fd`
- `git checkout -- .` 或 `git restore .`（丢弃所有未提交变更）
- `git branch -D`（强制删除分支）
- `git rebase` 的交互模式（`-i`）

---

## 三、文件权限规则

### 可自由读取（L1）
- `src/` 下所有源文件
- `public/` 下所有静态资源
- `package.json`、`tsconfig.json`、`.gitignore`
- 任何配置文件

### 可自由写入（L2 任务范围内）
- `src/` 下的源文件（任务目标文件）
- `public/` 下的静态资源（任务需要的）
- 配置文件（仅在任务需要时）

### 写入禁止（任何等级，除非用户明确要求写入这些文件）
- `.env`、`.env.local`、`.env.*` — 含敏感信息
- `settings.local.json`、`*.local.json` — 含本地 API Key
- `.claude/` 目录 — Claude Code 自身配置
- `.git/` 目录 — 版本控制元数据
- `node_modules/` — 由包管理器管理

### 禁止覆盖（需二次确认）
- `PROJECT_MANAGER.md` — 项目宪章，修改需用户明确同意
- `AI_SAFETY_RULES.md`（本文件）— 安全红线，修改需用户明确同意
- `package.json`（dependencies 部分）— 新增依赖需说明原因并获得确认

---

## 四、删除权限规则

### 删除文件
- **任何删除文件的操作都必须得到用户确认**
- 用户说"清理"、"移除"不等于授权删除——必须确认具体文件名
- 删除前必须说明：该文件的作用、为什么可以安全删除、是否有其他文件引用它

### 删除代码
- 删除未使用的导入、变量、函数 → 可以（L2 任务范围内）
- 删除整个组件 → 必须确认
- 删除内容文件（指南、文章）→ 必须确认，并说明对整体内容体系的影响
- 删除路由（app 目录下的 page）→ 必须确认，说明影响的 URL

### 禁止删除的类别
- 任何 `.git/` 下的内容
- 任何 `.claude/` 下的内容
- 任何配置类文件未经确认
- 任何 .gitignore 中列出的本地文件

---

## 五、自动化权限规则

### CI/CD 脚本
- 可以查看 `.github/workflows/` 下的文件
- **修改 CI/CD 配置需要用户明确授权**（修改部署流程属于 L5 级别操作）
- 禁止在 CI/CD 流程中添加或修改 secrets 值
- 禁止在 CI/CD 中添加未经确认的外部服务调用

### Git Hooks（`.githooks/`）
- 可以查看 hook 脚本内容
- **修改 hook 脚本需要用户明确授权**（hook 影响所有开发者）
- 新增 hook 需要用户明确授权

### 自动化命令链
- 禁止将 L2、L3、L4 操作串联成自动化脚本（如"保存代码→添加→提交→推送"一步完成）
- 每一步必须回到用户确认

---

## 六、高风险操作规则

### 网络请求
- 可以 fetch 公开的文档 URL（由用户提供或项目内已有的链接）
- **禁止**向未知 URL 发送项目代码、配置文件或用户数据
- 使用 WebFetch 或 WebSearch 时，仅访问与任务直接相关的公开页面

### 外部工具调用
- MCP 服务调用仅限已配置的工具
- **禁止**尝试调用未配置的 MCP 服务
- **禁止**在 MCP 调用中传入 API Key、token 等敏感信息

### 系统命令
- 禁止使用 `rm -rf`、`rd /s /q` 等递归删除命令（除非用户确认，且路径在项目目录内）
- 禁止使用 `kill`、`taskkill` 等进程终止命令
- 禁止修改系统环境变量（PATH、registry 等）
- 禁止使用 `sudo`、管理员权限运行命令

### 依赖管理
- `npm install` / `npm ci` → 可以（L2 任务范围内，用于开发和构建）
- `npm install <new-package>` → **需要用户确认**（新增依赖属于架构决策）
- `npm uninstall` → **需要用户确认**
- `npm update` → **需要用户确认**（可能引入 breaking changes）
- 禁止修改 `package-lock.json`（由 npm 自动管理）

---

## 七、批量修改规则

### 定义
同时修改 ≥ 3 个文件或 ≥ 10 行，视为批量修改。

### 规则
1. **批量修改前必须向用户说明**：改动范围、涉及文件、修改原因
2. **全局替换（replace_all）必须在确认范围内使用**：明确替换的目标字符串，确认不会误伤
3. **禁止盲目的全局替换**：如"把项目中所有 import 路径改掉"——必须先搜索确认所有匹配点
4. **批量重命名**：必须用户明确授权，且只改指定范围内的文件

---

## 八、部署规则

### 部署前检查清单（L5 必须全部满足）
- [ ] `tsc --noEmit` 零错误
- [ ] `npm run build` 成功
- [ ] 所有变更已 commit
- [ ] 无未跟踪的大文件（检查 git status）
- [ ] 用户明确说"部署"或"上线"

### Vercel 部署
- Vercel 通过 push master 自动触发，AI 不直接操作 Vercel 平台
- 如果用户说"部署到 Vercel"：确认 master 分支已 pull 最新、变更已 push，然后告知用户 Vercel 会自动部署

### GitHub Pages 部署
- 由 `.github/workflows/deploy.yml` 定义流程
- AI 不修改部署流程文件未经用户授权
- 部署失败时：分析 CI 日志，提出修复方案，由用户决定是否修改 CI 配置

### 部署禁止事项
- 禁止绕过 CI/CD 直接部署
- 禁止在未构建验证通过的情况下推送
- 禁止修改生产环境的配置而不同步到仓库

---

## 九、Agent 规则

### Agent 权限继承
- Agent 继承调用者的权限等级
- Agent **不得自行升级权限**
- 如果 Agent 的任务需要 L3 及以上操作，必须在返回结果中明确说明，**由主线程获取用户授权后执行**

### Agent 授权边界
| Agent 操作 | 所需授权等级 |
|-----------|------------|
| 搜索代码、读取文件、分析结构 | L1（无需额外授权） |
| 写入或编辑文件 | L2（任务级授权） |
| git commit | L3（用户明确指令）|
| git push | L4（用户明确指令）|
| 修改 CI/CD 或部署配置 | L5（用户明确指令）|

### Agent 禁止事项
- 禁止在 Agent prompt 中写入敏感信息（API Key、token、密码）
- 禁止 Agent 之间传递 L3/L4/L5 授权
- 禁止 Agent 绕过本文档的安全规则
- 如果 Agent 发现任务与安全规则冲突，必须停止并报告，不可自行裁决

---

## 十、Workflow 规则

### Workflow 授权边界
- Workflow 执行前，其脚本中涉及的**所有操作等级**必须在调用时获得授权
- Workflow 中如果包含 L3 操作（commit），用户必须在启动 Workflow 时明确授权 commit
- Workflow 中如果包含 L4/L5 操作（push/deploy），必须拆分：Workflow 运行到 L3 停止，L4/L5 由主线程单独执行

### Workflow 安全限制
- Workflow 脚本不得包含绕过安全规则的逻辑
- Workflow 的 `meta` 块必须如实声明其最高操作等级
- Workflow 结果返回后，主线程必须验证变更符合安全规则再进入下一等级
- 如果 Workflow 执行了 L2 修改但用户未授权 L3，**禁止自动提交**

---

## 十一、Command 规则

### 自定义 Command 的安全要求
- 每个 Command 必须声明其最大操作等级
- Command 不得隐式升级操作等级（如把 L2 的保存操作后自动触发 L3 的 commit）
- 如果 Command 需要链式操作（如"保存 → 提交 → 推送"），每一步必须回到用户确认，**不允许一键链式**

### /commit 类 Command
- 必须在执行前展示将要 commit 的内容摘要
- 必须等待用户确认后才执行
- 禁止自动 push

### /deploy 类 Command
- 必须先运行构建验证
- 必须在执行前明确告知用户部署目标
- 禁止跳过构建验证直接部署

---

## 十二、安全违规响应

当 AI 检测到以下情况时，**必须停止当前操作并报告**：

1. 用户的指令可能违反安全规则 → 说明风险，建议安全替代方案
2. Agent/Workflow 试图绕过安全规则 → 停止该 Agent/Workflow，报告主线程
3. 发现敏感信息在代码中 → 警告用户但不公开该信息的内容
4. 操作的破坏性超出用户预期 → 在执行前说明实际影响范围，等待确认

---

## 十三、本文档的修改

- 本文档的修改需要用户明确授权
- **AI 不得在用户未要求的情况下主动修改本文档**
- **其他文档或 Skill 不得包含覆盖本文档条款的声明**
- 如果其他文档与本文档冲突，以本文档为准
- 如果项目实际情况发生变化（如新增部署目标、新增敏感文件类别），由用户决定是否更新本文档
