# PROJECT_MANAGER.md

---

## 一、项目定位

**AI Tools Guide（Robin Tools）** 是面向中国开发者的 AI 编程工具一站式配置指南。

- **一句话定义**：让国内开发者用最短路径完成 AI 编程工具的安装、配置和排错，减少因网络限制和配置错误导致的挫败。
- **行业位置**：教学辅助型内容站，不是工具本身、不是 SaaS、不是社区平台。
- **覆盖范围**：主流 AI 编程工具的安装、配置、故障排查、使用教程，以及跨工具的协议机制、安全策略和实战经验。
- **不覆盖范围**：AI 模型本身的训练/微调、非编程类 AI 工具、英文版内容、商业培训服务。

---

## 二、核心价值

1. **可复制性**：每一步给出的命令可直接复制执行，不隐含前置条件、不省略参数。
2. **网络真实性**：明确标注每个步骤在国内网络环境下的实际情况——直连、需中转、需科学上网，不假装无障碍。
3. **跨平台完整性**：Windows、macOS、Linux 三端均有对应命令和说明，不偏废任一端。
4. **信息准确性**：配置字段名、命令、版本号与官方文档保持一致。价格、功能描述标注验证时间。
5. **克制美学**：设计服务于阅读，不服务于展示。液态玻璃是工程能力的签名，不是营销噱头。

---

## 三、目标用户

### 主要用户
国内 AI 开发工具使用者：开发者、技术爱好者、学生。

### 共同特征
- 中文为主要阅读语言
- 知道 AI 编程工具但不知道如何开始配置
- 遇到网络问题会放弃安装
- 移动端和桌面端均访问

### 用户场景（按优先级）
1. 新用户首次安装 AI 编程工具 → 从首页找到对应工具 → 跟随安装指南操作
2. 已安装但无法使用 → 查看配置指南 → 按步骤接入模型 API
3. 遇到报错 → 搜索故障排查页 → 找到对应错误和解决方案
4. 想深入了解使用技巧 → 阅读教程或技术笔记
5. 几个工具之间纠结 → 查看对比表

### 非目标用户
- 英文为主要语言的海外开发者
- 完全不需要 AI 工具的纯手工开发者
- 已有高级自定义配置能力的资深用户（不在核心路径上，但内容可作为参考）

---

## 四、内容战略

### 内容类型
| 类型 | 定位 | 更新频率要求 |
|------|------|-------------|
| 工具指南（安装/配置/排查/教程） | 核心产品，高准确度 | 当工具或 API 有重大变更时更新 |
| 技术笔记 | 深度补充，实战经验 | 新增即可，回查时效性每季度 |
| 工具对比表 | 选型参考 | 当工具价格或功能有重大变更时更新 |
| 外部资源链接 | 辅助导航 | 链接失效时更新 |

### 内容质量标准（全类型通用）
- 命令可直接复制，不含注释干扰，不含 `$` / `>` 等 shell 提示符
- 涉及网络的步骤，必须说明国内直连情况，必要时给出备选方案
- 价格、版本号、API 接口地址必须标注验证时间，并附时效性提示
- 平台特有内容必须用条件分支隔离，不跨平台泄漏
- 代码块必须标注语言，提供一键复制
- 大写专业术语保持统一（API Key 不是 apikey，VS Code 不是 vscode）
- 避免营销用语：不用"只需三步"、"超简单"、"一键搞定"

### 内容结构约定
- 指南：标题 → 简介 → 步骤式正文（每步有编号锚点）→ 验证方法 → 费用/后续提醒
- 技术笔记：标题 → 摘要 → 日期与标签 → 正文 → 外部链接
- 对比表：维度行 × 工具列，中立呈现差异，底部给出选型建议但不过分推荐单一工具

### 时效性管理
- 所有涉及价格、版本号、API 配置的内容，必须在下方标注类似"以上信息验证于 YYYY-MM"的提示
- 每季度批量检查一次所有内容页的时效性
- 对比表（`compare-data.ts`）在编辑任一工具指南时顺带检查，优先校验"扩展生态""文件操作""AI 模型"等会随时间变化的列
- 发现过时内容时优先更新，更新完成后更新验证日期

---

## 五、功能战略

### 已确立的功能方向
1. **三层信息架构**：首页（发现） → 工具页（选择指南类型） → 指南详情（步骤执行）
2. **全局搜索**：Cmd/Ctrl+K 唤起，覆盖工具名、指南名、步骤名、文章标题
3. **跨平台切换**：在指南内容中一键切换 Windows/macOS/Linux 视图
4. **双主题支持**：暗色/亮色，用户手动切换，持久化偏好

### 功能边界
**属于本项目**：
- 工具安装和配置的静态内容呈现
- 搜索和导航
- 阅读辅助（目录、进度条、外部链接）

**不属于本项目**：
- 用户系统（注册、登录、偏好云同步）
- 评论或社区讨论
- 实时更新（价格 API、版本检测）
- 交互式配置生成器
- 多语言版本（i18n 为远期方向，当前不做）

### 功能新增的判断标准
新增功能前，回答以下问题：
0. **定位前置**：新增内容与现有结构中哪个功能重叠？应并列还是做子集？（先定位再放位置，不按"新型=新节"惯性放置）
1. 是否直接服务于"帮用户完成配置"这一核心任务？
2. 是否可以用纯静态方式实现（无需数据库、无需后端）？
3. 是否增加现有内容的维护负担？
4. 是否在移动端同样可用？

三个"是"才启动。

---

## 六、质量标准

### 代码质量
- TypeScript strict 模式，零类型错误
- ESLint 零警告
- 全量构建通过（`next build` 成功）
- 所有动态路由有 `generateStaticParams`，无运行时 404

### 内容质量
- 命令可直接复制执行
- 平台特有内容不跨平台泄漏
- 时效性信息有标注
- 无错别字、无格式混乱

### UI 质量
- 暗色/亮色双主题下所有文字可读（WCAG AA 对比度）
- 移动端（375px 宽）内容可完整展示，无横向溢出
- 触摸设备 hover 效果正常降级
- 无控制台错误或 React hydration warning（Dark Reader 等浏览器扩展造成者除外）
- 行高响应式：移动端 1.55 → 平板端 1.5 → 桌面端 1.45，正文段落 ≤ 1.65
- 液态玻璃层级规范：
  | 层级 | variant | blur | shine | 适用场景 |
  |------|---------|------|-------|---------|
  | 主视觉引导 | heavy | 4px | heavy | Hero CTA、首页大卡片 |
  | 内容容器 | medium | 14px | medium | 指南页内容区、Tab 栏、对比表 |
  | 辅助元素 | light | 12px | light | Callout、Tooltip、弹窗面板 |
  | 无玻璃 | — | — | — | 代码块、纯文本、表格行（避免过度玻璃） |

### SEO 质量
- 每页有独立 title + description
- 所有页面在 sitemap.xml 中
- 语义化 HTML 结构完整

---

## 七、技术决策原则

1. **静态优先**：优先选择不需要服务端运行的方案。功能能用静态 HTML/CSS/JS 解决的不引入运行时。
2. **内容驱动架构**：内容结构决定组件结构，不是反过来。新增内容类型时才新增组件类型。
3. **CSS 变量为唯一颜色源**：任何颜色的 selector 只有一个——CSS 自定义属性。主题切换只交换变量值。
4. **渐进增强**：核心功能在所有浏览器可用。滤镜、动画等视觉效果支持降级。
5. **平台感知是内容问题**：平台差异在内容层解决（渲染函数参数），不在路由层或组件层引入分支。
6. **偏好显式**：不猜测用户的平台、主题、语言——提供切换控件，用户自己选择。

---

## 八、需求优先级评分体系

每个需求从以下五个维度打分（1-5 分），加权计算总分。

| 维度 | 权重 | 5 分标准 | 1 分标准 |
|------|------|----------|----------|
| **用户价值** | 30% | 直接解决多数用户当前遇到的卡点 | 只有极少数用户可能受益 |
| **长期价值** | 20% | 持续产生价值，不依赖时效性 | 一次性价值或很快过期 |
| **维护成本** | 15% | 几乎不需要后续维护 | 需要持续人工更新 |
| **SEO 价值** | 15% | 覆盖高搜索量的核心长尾词 | 对搜索引擎发现无贡献 |
| **开发成本** | 20% | 半天内可完成 | 需要 3 天以上或涉及架构变更 |

### 评分公式
```
总分 = 用户价值×0.3 + 长期价值×0.2 + (6-维护成本)×0.15 + SEO价值×0.15 + (6-开发成本)×0.2
```
（维护成本和开发成本逆向计分：成本越低得分越高）

### 优先级判定
- ≥ 4.0：立即做（当前版本）
- 3.0 - 3.9：下个版本
- 2.0 - 2.9：远期规划
- < 2.0：不做

### 额外加权规则
- 安全问题直接升为最高优先级（无论评分）
- 内容过时修复 = 用户价值自动 +1（已有用户被错误信息误导）
- 影响多个工具的内容更新优先于单工具内容

---

## 九、技术债处理原则

### 什么是技术债（本项目定义）
- 绕过既定架构的临时方案
- 被多个地方复制但未抽取的重复逻辑
- 类型标注用 `any` 绕过的地方
- 已弃用但未删除的代码路径

### 处理规则
1. **新功能不引入新债**：如果实现新功能需要绕过当前架构，先评估是否重构架构，而不是临时打补丁。
2. **修改文件时清理该文件的技术债**：打开一个文件做修改，顺手清理该文件内的重复代码和 `any` 类型。
3. **不单独安排"技术债清理版本"**：技术债在正常迭代中附带清理，不单独立项。
4. **允许的债**：对即将废弃的功能不做重构；对一次性脚本不加类型约束。

---

## 十、AI 协作原则

1. **先理解再动手**：接到任务后，先分析任务影响范围，定位关键代码，确认理解无误后再修改。
2. **最小变更**：只改任务要求改的东西。不顺手重构、不加"以后可能有用"的抽象、不改任务范围外的文件。
3. **用现有模式**：新增代码的风格、命名、结构应与同目录下现有代码一致。不准发明新模式。
4. **用工具验证**：声称完成前，运行 TypeScript 类型检查和构建。看到通过才算。
5. **内容用中文**：所有面向用户的文字必须是中文。代码注释仅在解释非显而易见的逻辑时才用英文写。
6. **不确定就问**：如果任务的实现路径有歧义，或可能违反本文档的原则，先问，不猜。

---

## 十一、禁止事项

### 设计禁区
- 大图大字少内容的 SaaS 营销页风格
- 过度动画（干扰阅读流程）
- 五颜六色的装饰性元素
- 促销/保证类营销用语（"100%成功"、"最全"、"最好"）
- AI slop 设计模式：侧条边框、彩色渐变文字、>12px 超大圆角、手绘 SVG、对角线条纹、无意义的发光边框

### 技术禁区
- 引入新的 UI 框架或组件库（当前技术栈已充分覆盖需求）
- 在组件中硬编码颜色值（必须通过 CSS 变量引用）
- 使用 Pages Router 或旧版 Next.js 模式
- 重新引入已弃用的 MDX 内容方案

### 内容禁区
- 英文作为主要语言（代码、命令、技术术语除外）
- 未经时效性标注的价格、版本号、API 地址
- 跨平台泄漏的内容（在 Windows 步骤中显示 macOS 命令）
- 可直接复制的代码中包含注释或 shell 提示符
- 带感情色彩的标签词描述方案的可用性状态（如"紧急备用""最后的稻草"；应改为"已标记弃用，仍可用""当其他方式不可行时可选此方式"等中立表述）

### Git 禁区
- 未经用户明确同意的 force push
- 未经用户明确同意的 git reset --hard
- 未经用户明确同意的 commit amend
- 跳过 hooks（`--no-verify`、`--no-gpg-sign`）

### AI 行为禁区
- 在不确定时自行做架构决策（应询问用户）
- 修改 `PROJECT_MANAGER.md` 或 `AI_SAFETY_RULES.md` 未经用户明确同意
- 声称"完成"但未运行验证命令
- 为一次性任务创建可复用抽象
