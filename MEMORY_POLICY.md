# MEMORY_POLICY.md

> **文件定位**：本文档是非强制加载的行为指导文件，不作为安全/架构标准。Claude 在处理"是否应该记住这件事"的决策时，参考本文档的分类标准。
>
> **加载方式**：不加入 CLAUDE.md 的强制加载列表。在 Claude 需要判断是否保存记忆时，由 Claude 自行查阅。

---

## 一、哪些内容进入长期记忆

### 1.1 完整 bug 排错流程（最高优先级）

满足以下**全部条件**的排错过程必须存入记忆：

- 排查步骤 ≥ 3 步，且每步之间有因果逻辑链（"做了 A → 观察到 B → 推测 C → 验证 D"）
- 最终找到了明确的根因（不是"不知道为什么就好了"）
- 过程中至少有一次"我以为……但其实……"的反直觉发现

**存储格式**：
```
标题：<问题现象的一句话描述>
内容：
  1. 现象：<用户看到了什么>
  2. 错误路径：<最初以为的原因 → 为什么错误>
  3. 排查链：A → B → C → D（每步的观察和推断）
  4. 根因：<最终确认的原因>
  5. 修复：<做了什么>
  6. 教训：<如果下次遇到类似问题，第一反应应该是什么>
标签：[排错] [涉及的技术领域] [严重程度]
```

### 1.2 反直觉的发现

任何让开发者说出"这不对啊""怎么会这样""原来如此"的发现。

- 框架/工具的行为与文档描述不一致
- 平台特有的怪异行为（Windows vs macOS vs Linux 差异）
- 版本升级导致的 breaking change（官方 changelog 未提及的）

### 1.3 用户矫正后的行为规则

用户明确纠正过的做事方式，附带原因。

### 1.4 架构决策的约束条件

"我们选了 X 而不是 Y，因为 Z"——且 Z 这个约束条件不是显而易见的。

反例（不存）："我们用 Next.js 因为它是 React 框架"——显而易见。
正例（存入）："我们不用 MDX 因为已弃用且内容需要平台感知渲染"——有具体的背景约束。

### 1.5 外部资源的定位

某个问题的最佳答案不在项目代码中，而在特定的外部链接、文档页面、视频教程中。

---

## 二、哪些内容绝不存入长期记忆

### 2.1 可从代码自动获取的信息

- 文件路径、目录结构 → `ls` / Glob 可获取
- 组件名称、函数签名 → Grep 可获取
- 依赖版本号 → `package.json` 可获取
- 配置项的具体值 → 配置文件可获取

### 2.2 单次执行过程

- "今天运行了 npm install，装了 3 个包" → 丢弃
- "今天 commit 了 5 次" → 丢弃
- 一次性脚本的输出结果 → 丢弃

### 2.3 简单错误（无排错价值）

- 拼写错误导致的 bug（改一个字就修复）
- 忘记 import 导致的报错
- 纯机械操作（"少了一个逗号""多了一个空格"）

### 2.4 已被后续修复覆盖的中间态

如果一个 bug 的排查过程最终发现是另一个已存记忆的 bug 的同一个根因，不重复存储。更新已有记忆的"关联问题"列表。

---

## 三、记忆分类与分片存储规则

### 3.1 五种记忆类型

| 类型 | 用途 | 文件命名 | 触发条件 |
|------|------|---------|---------|
| `feedback` | 用户矫正的行为规则 | `feedback-<主题>.md` | 用户说"不对""不要""以后都" |
| `reference` | 外部资源位置 | `reference-<资源名>.md` | 发现不在项目内的关键信息源 |
| `project` | 项目决策/约束 | `project-<决策主题>.md` | 做了有长期影响的架构选择 |
| `user` | 用户偏好/习惯 | `user-<偏好主题>.md` | 发现影响协作方式的用户特征 |
| `bug` | 排错全流程记录 | `bug-<现象关键词>.md` | 满足 1.1 的条件 |

### 3.2 记忆分片原则

- **一个记忆文件 = 一个独立事件**。不把两次不同的排错过程合并到一个文件。
- **关联用 `[[文件名]]` 链接**。如果 bug-A 的根因与 bug-B 有关，在各自文件中互相链接。
- **每类记忆有独立目录**（如果数量增长到 ≥ 5 个）。目录名用类型名：
  ```
  memory/
  ├── feedback/
  ├── reference/
  ├── project/
  ├── user/
  ├── bug/
  └── MEMORY.md              # 索引
  ```
- **MEMORY.md 是索引而非内容**。每条一行，≤ 150 字符，含链接和一句话描述。

### 3.3 排错记忆的特殊格式

排错记忆（`bug-*.md`）比其他类型多一个必填字段：`severity`（严重程度）。

```
severity: critical | high | medium | low

critical: 导致服务不可用、数据丢失、安全漏洞
high:     导致核心功能无法使用但可绕过
medium:   影响体验但不阻塞使用
low:      视觉效果、边缘场景
```

严重程度决定后续是否值得转化成文章（见第四节）。

---

## 四、排错记忆 → 复盘素材 → 技术文章的转化链路

### 4.1 转化判断标准

| 条件 | 转化方向 |
|------|---------|
| severity = critical 或 high，排查步骤 ≥ 5 步 | **完整技术文章** |
| severity = critical 或 high，排查步骤 3-4 步 | **短笔记** |
| severity = medium，排查过程有反直觉发现 | **短笔记** |
| severity = medium，排查过程无特殊发现 | 保留为素材，等关联事件积累后合并 |
| severity = low | 仅保留记忆，不转化 |

### 4.2 转化流程

```
排错完成
  ↓
存入 bug-<现象关键词>.md          ← 第一步：完整记录
  ↓
标记 severity + 关联标签
  ↓
当同一技术领域积累 ≥ 2 条记忆时    ← 第二步：等待关联
  ↓
回顾记忆 → 判断是否值得成文
  ↓
  ├─ 值得 → 生成文章草稿到 文章草稿/ 目录
  │          标题格式：<技术领域>踩坑集合：<现象1> + <现象2>
  │          结构：每个坑 = 现象 → 错误路径 → 排查链 → 根因 → 修复 → 教训
  │          尾部：统一教训总结 + 排查方法论提炼
  │
  └─ 不值得 → 在 MEMORY.md 中加标记 #素材已归档
```

### 4.3 文章草稿质量要求

从排错记忆转化来的文章，必须满足：

- **根因分析完整**：不只说"做了什么修复了"，必须说"为什么这样做修复了"
- **可复现**：读者按照描述的步骤配置相同的环境，应能复现该问题
- **有方法论提炼**：不只讲这一个 bug，要抽象出这类问题的排查思路
- **有环境约束说明**：明确标注这个问题在什么条件下会发生、什么条件下不会

### 4.4 积累触发

当以下条件之一满足时，Claude 应主动提醒用户"是否考虑整理成文章"：

- 同一技术领域（如"CSS 特异性""MCP 配置""主题切换"）的 bug 记忆 ≥ 3 条
- 某条记忆的 severity = critical 且排查步骤 ≥ 5 步
- 用户在使用 `/audit` 时发现相关内容的时效性问题

---

### 补充：与 CLAUDE.md 的关系

```
CLAUDE.md 启动时强制加载 → 不包含本文档（本文档非强制）

当 Claude 遇到以下场景时，自行查阅本文档：
  · 用户说"记住这个"
  · 排错过程结束，判断是否存入记忆
  · 用户说"之前那个 bug 是怎么解决的"
  · 用户问"这些素材能不能写篇文章"
```
